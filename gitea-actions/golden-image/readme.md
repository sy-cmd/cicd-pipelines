+ This git gitAction builds the image from the Dockerfile and pushes it the our registry 

+ we need to check with the runner if it can reach the registry if not we can manually the insecurities to the runner 